
const intro=document.querySelector('#brand-intro');const introDuration=4200;const reducedMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;let introSeen=false;try{introSeen=sessionStorage.getItem('ma-intro-seen')==='1';}catch(_){}function markIntroSeen(){try{sessionStorage.setItem('ma-intro-seen','1');}catch(_){}}function finishIntro({immediate=false}={}){document.body.classList.remove('intro-active');document.documentElement.classList.remove('skip-intro');if(!intro)return;if(immediate){intro.remove();return;}intro.classList.add('is-finished');window.setTimeout(()=>intro.remove(),900);}if(reducedMotion||!intro||introSeen){finishIntro({immediate:true});}else{markIntroSeen();window.setTimeout(()=>finishIntro(),introDuration);}const main=document.querySelector('main');const footer=document.querySelector('.site-footer');const menuToggle=document.querySelector('.ma-menu-toggle');const mobileMenu=document.querySelector('#ma-mobile-menu');const menuClose=mobileMenu?.querySelector('[data-menu-close]');const contactModal=document.querySelector('#contact-modal');const infoModal=document.querySelector('#info-modal');const contactCloseButtons=[...document.querySelectorAll('[data-contact-close]')];const infoCloseButtons=[...document.querySelectorAll('[data-info-close]')];const infoBody=infoModal?.querySelector('[data-info-body]');const contactForm=contactModal?.querySelector('form');const contactServiceSelect=contactForm?.querySelector('select[name="service"]');let lastMenuFocus=null,lastContactFocus=null,lastInfoFocus=null;if(mobileMenu&&'inert'in HTMLElement.prototype)mobileMenu.inert=true;if(contactModal&&'inert'in HTMLElement.prototype)contactModal.inert=true;if(infoModal&&'inert'in HTMLElement.prototype)infoModal.inert=true;function getFocusable(container){if(!container)return[];return[...container.querySelectorAll('a[href], button:not([disabled]), input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex="-1"])')].filter((e)=>!e.hasAttribute('hidden')&&e.offsetParent!==null);}function overlayOpen(){return Boolean(mobileMenu?.classList.contains('is-open')||contactModal?.classList.contains('is-open')||infoModal?.classList.contains('is-open'));}function updatePageInert(){const open=overlayOpen();if('inert'in HTMLElement.prototype){if(main)main.inert=open;if(footer)footer.inert=open;}}function updateScrollLock(){document.body.classList.toggle('menu-open',Boolean(mobileMenu?.classList.contains('is-open')));document.body.classList.toggle('modal-open',Boolean(contactModal?.classList.contains('is-open')||infoModal?.classList.contains('is-open')));updatePageInert();}function openMenu(){if(!mobileMenu||!menuToggle)return;closeContactModal({restoreFocus:false});closeInfoModal({restoreFocus:false});lastMenuFocus=document.activeElement;if('inert'in HTMLElement.prototype)mobileMenu.inert=false;mobileMenu.classList.add('is-open');mobileMenu.setAttribute('aria-hidden','false');menuToggle.setAttribute('aria-expanded','true');updateScrollLock();window.requestAnimationFrame(()=> (menuClose||getFocusable(mobileMenu)[0])?.focus());}function closeMenu({restoreFocus=true}={}){if(!mobileMenu||!menuToggle)return;const wasOpen=mobileMenu.classList.contains('is-open');mobileMenu.classList.remove('is-open');mobileMenu.setAttribute('aria-hidden','true');if('inert'in HTMLElement.prototype)mobileMenu.inert=true;menuToggle.setAttribute('aria-expanded','false');updateScrollLock();if(restoreFocus&&wasOpen)(lastMenuFocus instanceof HTMLElement?lastMenuFocus:menuToggle).focus();}function normalizeServiceLabel(v=''){return v.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/&/g,' and ').replace(/[^a-z0-9]+/g,' ').trim();}function setContactService(value=''){if(!contactServiceSelect||!value)return;const options=[...contactServiceSelect.options].filter((o)=>o.value);const exact=options.find((o)=>o.value===value);if(exact){contactServiceSelect.value=exact.value;return;}const wanted=normalizeServiceLabel(value);const aliasGroups=[['marketing strategy','marketingova strategia','marketingova strategie'],['social media management and production','social media management production','sprava socialnych sieti produkcia','sprava socialnich siti produkce'],['public relations','influencer collaborations','influencer spoluprace','influencer collaborations and pr'],['web design','website creation','tvorba webu'],['web audit','website audit','webovy audit']];const matched=options.find((o)=>{const n=normalizeServiceLabel(o.value);if(n===wanted)return true;return aliasGroups.some((group)=>group.includes(wanted)&&group.includes(n));});if(matched)contactServiceSelect.value=matched.value;}function openContactModal(trigger=null,presetService=''){if(!contactModal)return;closeMenu({restoreFocus:false});closeInfoModal({restoreFocus:false});lastContactFocus=trigger||document.activeElement;if('inert'in HTMLElement.prototype)contactModal.inert=false;contactModal.classList.add('is-open');contactModal.setAttribute('aria-hidden','false');if(presetService)setContactService(presetService);updateScrollLock();window.setTimeout(()=>{const preferred=contactModal.querySelector('input[name="name"]');(preferred||getFocusable(contactModal)[0])?.focus({preventScroll:true});},60);}function closeContactModal({restoreFocus=true}={}){if(!contactModal)return;const wasOpen=contactModal.classList.contains('is-open');contactModal.classList.remove('is-open');contactModal.setAttribute('aria-hidden','true');if('inert'in HTMLElement.prototype)contactModal.inert=true;updateScrollLock();if(restoreFocus&&wasOpen&&lastContactFocus instanceof HTMLElement)lastContactFocus.focus();}function openInfoModal(trigger){if(!infoModal||!infoBody||!trigger)return;const id=trigger.dataset.infoTemplate;const template=id?document.getElementById(id):null;if(!template)return;closeMenu({restoreFocus:false});closeContactModal({restoreFocus:false});lastInfoFocus=trigger;infoBody.innerHTML=template.innerHTML;if('inert'in HTMLElement.prototype)infoModal.inert=false;infoModal.classList.add('is-open');infoModal.setAttribute('aria-hidden','false');updateScrollLock();window.setTimeout(()=> (getFocusable(infoModal)[0])?.focus({preventScroll:true}),40);}function closeInfoModal({restoreFocus=true}={}){if(!infoModal)return;const wasOpen=infoModal.classList.contains('is-open');infoModal.classList.remove('is-open');infoModal.setAttribute('aria-hidden','true');if('inert'in HTMLElement.prototype)infoModal.inert=true;updateScrollLock();if(restoreFocus&&wasOpen&&lastInfoFocus instanceof HTMLElement)lastInfoFocus.focus();}menuToggle?.addEventListener('click',()=> mobileMenu?.classList.contains('is-open')?closeMenu():openMenu());menuClose?.addEventListener('click',()=>closeMenu());mobileMenu?.querySelectorAll('[data-menu-link]').forEach((link)=>{link.addEventListener('click',(e)=>{if(link.hasAttribute('data-contact-open')){e.preventDefault();openContactModal(link);return;}closeMenu({restoreFocus:false});});});document.querySelectorAll('[data-contact-open]').forEach((trigger)=>{if(mobileMenu?.contains(trigger))return;trigger.addEventListener('click',(e)=>{e.preventDefault();openContactModal(trigger);});});document.querySelectorAll('[data-info-open]').forEach((trigger)=>trigger.addEventListener('click',(e)=>{e.preventDefault();openInfoModal(trigger);}));contactCloseButtons.forEach((b)=>b.addEventListener('click',()=>closeContactModal()));infoCloseButtons.forEach((b)=>b.addEventListener('click',()=>closeInfoModal()));infoBody?.addEventListener('click',(e)=>{const btn=e.target.closest('[data-info-contact]');if(!btn)return;e.preventDefault();const value=btn.dataset.infoContact||'';closeInfoModal({restoreFocus:false});openContactModal(btn,value);});function trapFocus(event,container){if(event.key!=='Tab')return;const focusable=getFocusable(container);if(!focusable.length){event.preventDefault();return;}const first=focusable[0],last=focusable[focusable.length-1];if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus();}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus();}}document.addEventListener('keydown',(event)=>{if(event.key==='Escape'){if(infoModal?.classList.contains('is-open')){closeInfoModal();return;}if(contactModal?.classList.contains('is-open')){closeContactModal();return;}if(mobileMenu?.classList.contains('is-open'))closeMenu();}if(infoModal?.classList.contains('is-open'))trapFocus(event,infoModal);else if(contactModal?.classList.contains('is-open'))trapFocus(event,contactModal);else if(mobileMenu?.classList.contains('is-open'))trapFocus(event,mobileMenu);});window.addEventListener('resize',()=>{if(window.innerWidth>900)closeMenu({restoreFocus:false});});document.querySelectorAll('[data-language]').forEach((link)=>{link.addEventListener('click',()=>{const language=link.dataset.language;if(!['cs','sk','en'].includes(language))return;try{localStorage.setItem('ma-language',language);sessionStorage.setItem('ma-intro-seen','1');}catch(_){}});});const revealObserver=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting){entry.target.classList.add('in-view');revealObserver.unobserve(entry.target);}});},{threshold:.08,rootMargin:'0px 0px -4% 0px'});document.querySelectorAll('.reveal').forEach((element)=>revealObserver.observe(element));const progressBar=document.querySelector('.scroll-progress span');const parallaxItems=[...document.querySelectorAll('[data-parallax]')];let ticking=false;function updateScrollEffects(){const scrollTop=window.scrollY||document.documentElement.scrollTop;const scrollable=Math.max(1,document.documentElement.scrollHeight-window.innerHeight);if(progressBar)progressBar.style.transform=`scaleX(${Math.min(1,scrollTop/scrollable)})`;if(!reducedMotion){const viewportCenter=window.innerHeight/2;parallaxItems.forEach((item)=>{const factor=Number(item.dataset.parallax||0);const rect=item.getBoundingClientRect();const itemCenter=rect.top+rect.height/2;const offset=(itemCenter-viewportCenter)*factor;item.style.transform=`translate3d(0, ${offset.toFixed(2)}px, 0)`;});}ticking=false;}function requestScrollEffects(){if(!ticking){window.requestAnimationFrame(updateScrollEffects);ticking=true;}}window.addEventListener('scroll',requestScrollEffects,{passive:true});window.addEventListener('resize',requestScrollEffects);requestScrollEffects();if(window.matchMedia('(hover: hover) and (pointer: fine)').matches){document.querySelectorAll('.vip-service-card,.vip-logo-tile').forEach((card)=>{card.addEventListener('pointermove',(event)=>{const rect=card.getBoundingClientRect();card.style.setProperty('--mx',`${event.clientX-rect.left}px`);card.style.setProperty('--my',`${event.clientY-rect.top}px`);});});}const navLinks=[...document.querySelectorAll('[data-nav-link][href^="#"]')];const observedSections=[...new Set(navLinks.map((link)=>document.querySelector(link.getAttribute('href'))).filter(Boolean))];if(observedSections.length){const activeNavObserver=new IntersectionObserver((entries)=>{const visible=entries.filter((entry)=>entry.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(!visible)return;navLinks.forEach((link)=>{const active=link.getAttribute('href')===`#${visible.target.id}`;link.classList.toggle('active',active);});},{threshold:[.2,.45,.7],rootMargin:'-18% 0px -55% 0px'});observedSections.forEach((section)=>activeNavObserver.observe(section));}


// V11 service scenario, inspired by the scene-based Party Tank section.
document.querySelectorAll('[data-logo-placeholder]').forEach((link)=>link.addEventListener('click',(event)=>event.preventDefault()));
const serviceScenario=document.querySelector('[data-service-scenario]');
if(serviceScenario){
  const scenes=[...serviceScenario.querySelectorAll('[data-service-scene]')];
  const tabs=[...serviceScenario.querySelectorAll('[data-service-tab]')];
  const triggers=[...serviceScenario.querySelectorAll('[data-service-trigger]')];
  const currentLabel=serviceScenario.querySelector('[data-service-current]');
  const prev=serviceScenario.querySelector('[data-service-prev]');
  const next=serviceScenario.querySelector('[data-service-next]');
  let activeIndex=0;
  const isMobile=()=>window.matchMedia('(max-width: 760px)').matches;
  const viewport=serviceScenario.querySelector('.scenario-services__viewport');
  function syncViewportHeight(){
    if(!viewport)return;
    const activeScene=scenes[activeIndex];
    if(!activeScene)return;
    if(isMobile()){
      window.requestAnimationFrame(()=>{
        viewport.style.height=`${activeScene.offsetHeight}px`;
        viewport.style.minHeight=`${activeScene.offsetHeight}px`;
      });
    }else{
      viewport.style.removeProperty('height');
      viewport.style.removeProperty('min-height');
    }
  }
  function setScene(index,{scroll=false}={}){
    const count=scenes.length;
    activeIndex=(index+count)%count;
    scenes.forEach((scene,i)=>{const active=i===activeIndex;scene.classList.toggle('is-active',active);scene.setAttribute('aria-hidden',String(!active));});
    tabs.forEach((tab,i)=>{const active=i===activeIndex;tab.classList.toggle('is-active',active);tab.setAttribute('aria-current',active?'true':'false');});
    if(currentLabel)currentLabel.textContent=String(activeIndex+1).padStart(2,'0');
    syncViewportHeight();
    if(scroll&&!isMobile()&&triggers[activeIndex])triggers[activeIndex].scrollIntoView({behavior:reducedMotion?'auto':'smooth',block:'center'});
  }
  tabs.forEach((tab,i)=>tab.addEventListener('click',()=>setScene(i,{scroll:true})));
  prev?.addEventListener('click',()=>setScene(activeIndex-1,{scroll:true}));
  next?.addEventListener('click',()=>setScene(activeIndex+1,{scroll:true}));
  if(triggers.length){
    const observer=new IntersectionObserver((entries)=>{
      if(isMobile())return;
      const visible=entries.filter((entry)=>entry.isIntersecting).sort((a,b)=>Math.abs(a.boundingClientRect.top-innerHeight/2)-Math.abs(b.boundingClientRect.top-innerHeight/2))[0];
      if(visible)setScene(Number(visible.target.dataset.serviceTrigger||0));
    },{threshold:.01,rootMargin:'-46% 0px -46% 0px'});
    triggers.forEach((trigger)=>observer.observe(trigger));
  }
  let startX=0;
  viewport?.addEventListener('touchstart',(event)=>{startX=event.changedTouches[0].clientX;},{passive:true});
  viewport?.addEventListener('touchend',(event)=>{const delta=event.changedTouches[0].clientX-startX;if(Math.abs(delta)>45)setScene(activeIndex+(delta<0?1:-1));},{passive:true});
  window.addEventListener('resize',syncViewportHeight,{passive:true});
  window.addEventListener('orientationchange',syncViewportHeight,{passive:true});
  if(document.fonts&&document.fonts.ready)document.fonts.ready.then(syncViewportHeight).catch(()=>{});
  setScene(0);
}
